# contact-book
a contact book that temporarily stores the number inputted from the user, it first ask for the number then name to save tat in a file called "text.txt" and later calling it with open()  
